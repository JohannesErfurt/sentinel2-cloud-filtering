[pytest]
testpaths = tests
addopts = -q -m "not slow"
markers =
    needs_product: requires a real .SAFE folder via --safe-dir
    slow: runs the real CLI end to end; excluded by default, run with -m slow
